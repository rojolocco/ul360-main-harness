[project]
name = "ul360-main-harness"
version = "0.1.0"
description = "Repository for the main harness of UL360"
readme = "README.md"
authors = [{ name = "rojolocco", email = "rojolocco@gmail.com" }]
requires-python = ">=3.14"
dependencies = []

[tool.uv.build-backend]
module-name = "harness"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = ["ipykernel>=7.3.0"]
